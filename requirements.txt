# Runtime (mirrors pyproject for Docker/CI simplicity)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
redis>=5.0.0
elasticsearch>=8.11.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
pyyaml>=6.0.1
httpx>=0.26.0

# Dev (CI installs these separately in workflow too)
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
